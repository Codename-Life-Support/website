{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open HTML in Microsoft Edge",
            "type": "msedge",
            "request": "launch",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
